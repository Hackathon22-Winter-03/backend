image: mysql
type: runtime

startup: |
  go mod download
  go build -o main
  mysql -u ${DB_USER} -p${DB_PASS} < ./sql/0_setup.sql
  mysql -u ${DB_USER} -p${DB_PASS} < ./sql/1_schema.sql
entrypoint: ./main -d

http_proxy: 8080
cname:
  turing-qomplete.trap.games

https: on
use_mariadb: true


